<Window x:Class="SimpleMvvmDemo.Client.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleMvvmDemo.Client"
        xmlns:prism="http://prismlibrary.com/" 
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="#FFD0DC6F">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Width="450" Height="50" Content="Save" FontSize="24"/>
        <TextBox Grid.Row="1" Margin="30 10" Text="{Binding Num1}" FontSize="24"/>
        <TextBox Grid.Row="2" Margin="30 10" Text="{Binding Num2}"  FontSize="24"/>
        <TextBox Grid.Row="3" Margin="30 10" Text="{Binding Num3}"  FontSize="24"/>
        <Button Grid.Row="4" Width="450" Height="50" Content="Add" FontSize="24" Command="{Binding DelegateCommand}" CommandParameter="Add"/>
    </Grid>
</Window>
